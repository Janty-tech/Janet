def main():
    x= int(input("What is the valueof x?"))
    if mod(x):
        print("Even")
    else:
        print("odd")



def mod(n):




        # `if n % 2 == 0:` is checking if the remainder of `n` divided by 2 is equal to 0. If the
        # remainder is 0, it means that `n` is an even number, and the function `mod` returns `True`.
        # Otherwise, it means that `n` is an odd number, and the function `mod` returns `False`.
        if n % 2 == 0:
            return True
        else:
            return false
        
main()